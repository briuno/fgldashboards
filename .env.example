# ==================================================================
# MODELO de variáveis de ambiente. Copie para os arquivos reais
# (ambos git-ignored) e preencha os valores.
# ==================================================================

# ------------------------------------------------------------------
# .env.local  → lido pelo app Next.js (lib/supabase/*). Valores PÚBLICOS.
# Projeto: ifjpzyqjdagnxygbkwpm
# ------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://ifjpzyqjdagnxygbkwpm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=cole_aqui_a_publishable_ou_anon_key

# ------------------------------------------------------------------
# .env  → lido só por scripts de operação locais (PowerShell → Management API).
# SEGREDOS — o app NÃO lê este arquivo.
# ------------------------------------------------------------------
SUPABASE_ACCESS_TOKEN=cole_aqui_o_personal_access_token   # começa com sb_secret_... (antigo sbp_...)
SUPABASE_PROJECT_REF=ifjpzyqjdagnxygbkwpm

# ------------------------------------------------------------------
# NÃO colocar aqui os segredos do Tier2. Eles vão como SECRETS no Supabase:
#   - TIER2_BASE_URL, TIER2_USERNAME, TIER2_PASSWORD  (via `supabase secrets set`)
# ------------------------------------------------------------------
